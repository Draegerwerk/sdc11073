If you want to use default certificates, put them into this folder.
Expected Filenames are sdccert.pem, userkey.pem and cacert.pem.
These certificates are used if there is no ssl context provided in constructor of device and/or client.